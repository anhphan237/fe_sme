import { PlaceholderPage } from '../../components/common/PlaceholderPage'

function Tasks() {
  return (
    <PlaceholderPage
      title="Tasks"
      subtitle="Track onboarding tasks and assignments by role."
      items={['HR task queue', 'Manager task list', 'Employee task checklist']}
    />
  )
}

export default Tasks
