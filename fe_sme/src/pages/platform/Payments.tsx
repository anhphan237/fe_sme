import { PlaceholderPage } from '../../components/common/PlaceholderPage'

function PlatformPayments() {
  return (
    <PlaceholderPage
      title="Payment Lookup"
      subtitle="Support tooling for payment troubleshooting."
      items={['Review payment attempts', 'Filter by status', 'Inspect transaction details']}
    />
  )
}

export default PlatformPayments
