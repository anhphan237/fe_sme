import { PlaceholderPage } from '../../components/common/PlaceholderPage'

function PlatformPlans() {
  return (
    <PlaceholderPage
      title="Plans"
      subtitle="Create and manage pricing plans."
      items={['Create or retire plans', 'Configure plan features', 'Set limits and tiers']}
    />
  )
}

export default PlatformPlans
